- for ns, i in namespaces
  .row
    .col-md-12.mms(id='creek-#{i}')
      h4.graph-title #{metric}


table.grid.table.table-condensed(style="margin-top: 20px;")
  thead
    tr
      th &nbsp;
      th #total/#lock
      th lock time
      th #readlock/#writelock
      th #read/#write
      th #insert/#update
  tbody
   - for ns in namespaces
    tr
      td #{ns}
      td #{deltas[ns + '.total.count']}/#{deltas[ns + '.lock.count']}
      td #{deltas[ns + '.lock.time']}µs
      td #{deltas[ns + '.readlock.count']}/#{deltas[ns + '.writelock.count']}
      td #{deltas[ns + '.read.count']}/#{deltas[ns + '.write.count']}
      td #{deltas[ns + '.insert.count']}/#{deltas[ns + '.update.count']}
