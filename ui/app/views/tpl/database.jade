include mixins/bytes

- stats = database.stats
- stats.extent_used_size = stats.document_size + stats.index_size
- stats.extent_free_size = stats.storage_size - stats.extent_used_size
- stats.extent_size = stats.storage_size / stats.extent_count
//- - host.disk_used = host.disk_total - host.disk_free

.row: .col-md-12: .databases
  .summary

.row
  .col-md-2
    .panel.padded
        ul.nav.nav-pills.nav-stacked
          li
            a.create-collection(title='create collection', href='#database/#{database.name}/collection') Collections
              span.glyphicon.glyphicon-plus-sign(style="padding-left: 10px; padding-top: 2px;")

          for collection_name in database.collection_names
            li
              a(href='#collection/#{database.name}/#{collection_name}')
                span #{collection_name}

  .col-md-4
    .panel.padded
      .donut


  .col-md-4

