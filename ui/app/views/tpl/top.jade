ol.breadcrumb
  unless context.deployment.type === 'standalone'
    li: a(href='#home') #{context.deployment.id}
  li: a(href='#home') #{context.instance.id}
  li top

.row
  .col-md-12
    p sampling...
    .matrix
